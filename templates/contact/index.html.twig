{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% form_theme contact_form 'bootstrap_5_layout.html.twig' %}

{% block body %}
    <div class='main-content p-5'>

        {# <h3>Il y a des erreurs dans le formulaire de contact</h3>

        <ul>
            {% for error in errors %}
                <li>{{ error.message }}</li>
            {% endfor %}
        </ul> #}

        {# afficher les messages flashs #}

        {% for message in app.flashes('comfirmation') %}
            <div class="alert alert-success">
                {{ message }}
            </div>
        {% endfor %}

        
        {{ form(contact_form, {attr: {'novalidate': 'novalidate'}}) }}

    </div>

{% endblock %}
